# ==========================================
# Dayflow HRMS - Backend Environment Configuration
# ==========================================

# Database Connection (MongoDB local or MongoDB Atlas)
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=dayflow_db

# Security & JWT Configuration
SECRET_KEY=amethyst-haze-dayflow-super-secret-key-2026
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# Email & SMTP Alert Configuration (Optional - falls back to DB notification logging if blank)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SENDER_EMAIL=noreply@dayflow-hrms.com
